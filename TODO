[Bugs]
UTF8 encoding (no html in the displayed name)
DONE: When closing group: creates two empty tabs

[Features]
Close tab link/button
Go to tab link/button
Sort tab order inside a group
Cancel/undo closing a group / tab
Tab selectable within a group
Re-order tab groups

[Design]
Thumbnail tab image instead of links (store in local and not online)
DONE: Highlight the active tab group
CSS during drag & drop

[General]
Help
Icon (ribbon, ...) in the header
